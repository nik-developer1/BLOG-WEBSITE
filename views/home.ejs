<%- include('partials/header'); -%>
<h1>Home</h1>
<p><%= para1 %></p>
<% blogs.forEach(function(element) { %>
    <% const post = element.content; %>
    <% const truncatedString = post.substring(0, 100); %>
    <h1> <%= element.title %> </h1>
    <span> <%= truncatedString %></span>
    <a href ="/posts/<%= element.title %>" >Read more</a>
<% }); %>
<%- include('partials/footer'); -%>
